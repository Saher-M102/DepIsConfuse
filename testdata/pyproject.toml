[project]
name = "test-project"
dependencies = [
  "requests>=2.28",
  "click",
  "this-toml-pkg-does-not-exist-xyz-999",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.poetry.dependencies]
python = "^3.9"
django = "^4.2"
